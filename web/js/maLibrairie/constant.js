var valtmp = (location.href).split('/');
var URI = valtmp[0]+"//"+valtmp[2]+"/eclat/";

var tabTuile = new Array();
tabTuile['id_tuile'] = URI + 'url_souhaiter';


/*
 * Gestion des constant de la librairie GRAPH
 */
var URI_AJAX_SESSION = URI + 'ajax/remplirSession/'

/*
 *  Fin constant GRAPH
 */

/*
 * Gestion des constant de la librairie gestion visuelle (accordeon)
 */
var affichageDefault = true; // par défaut on affiche

/*
 *  Fin constant GRAPH
 */

var id_reservware = 14;

